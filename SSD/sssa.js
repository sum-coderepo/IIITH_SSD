let data = {
    name:"Brendan Eich",
    inventor :"Javascrpt"
}
console.log(`The name of his is ${data.name}. He is the inventor of ${data.inventor} programming language`);


console.log(`The name of his is ${data.name}. He is the inventor of ${data.inventor} programming language`);

function add(a,b){
    return a+b;
}

let c = add(3,4)
console.log(c)